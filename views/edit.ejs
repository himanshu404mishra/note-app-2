<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Edit Task</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    </head>
    <body>
        <div class="main w-full min-h-screen bg-zinc-900 text-white p-10">
            <a class="text-blue-500 inline-block mb-10" href="/">Go back to Home</a>
            <form action="/edit" method="post">
                <input
                    class="block w-3/4 rounded-lg outline-none px-5 py-2 bg-zinc-800 text-zinc-500"
                    type="text"
                    placeholder="Previous Name"
                    name="previousName"
                    required
                    value="<%=previousName%>"
                    readonly
                />
                <input
                    class="block w-3/4 rounded-lg outline-none px-5 py-2 mt-2 bg-zinc-800"
                    type="text"
                    placeholder="New Name"
                    name="newName"
                    required
                />
                
                <input
                    class="mt-2 bg-yellow-600 px-5 py-3 rounded-md cursor-pointer"
                    type="submit"
                    value="Update Name"
                />
            </form>
        </div>
    </body>
</html>
